# Base URL of the StayMate backend.
# Use your computer's LAN IP (NOT localhost) so a physical phone can reach it.
# Find it with `ipconfig` (Windows) -> IPv4 Address.
# Example: http://192.168.1.10:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
